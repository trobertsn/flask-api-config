apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-api-chart.fullname" . }}
  labels:
    {{- include "flask-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flask-api-chart.selectorLabels" . | nindent 4 }}
